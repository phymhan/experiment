import numpy as np
import matplotlib.pyplot as plt
import torch


"""
plt.plot(y)
plt.xlabel('x')
plt.ylabel('y')
plt.title('title')
plt.legend()
plt.savefig('plot.png')
"""

"""
torchvision.utils.save_image(
    image_tensor,
    os.path.join("/path/to/save/", "image_name.png"),
    nrow=nrow,
    normalize=True,
    value_range=(-1, 1),
)
"""
